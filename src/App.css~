#root {
    display: flex;
    height: 100%;
    width: 100%;
    padding: clamp(10px, 2vw, 35px);
    gap: 20px;
}
img{
    display: block;
    max-width: 100%;
    object-fit: contain;
}
.card-container{
    display: flex;
    height: 100%;
    width: 100%;
    border: 3px solid black;
    padding: clamp(10px, 2vw, 30px);
    flex-direction: column;
    gap: 5px;
    overflow: auto;
    container-type: inline-size;
}
.header{
    display: flex;
    width: 100%;
    height: fit-content;
    border-top: 3px solid black;
    border-bottom: 3px solid black;
    justify-content: space-between;
    align-items: center;
    .date-container{
        display: flex;
        width: fit-content;
        background-color: black;
        height: 100%;
        color: white;
        justify-content: center;
        align-items: center;
        font-weight: 500;
        font-size: clamp(1.2rem, 2vw, 2rem);
        padding: 15px;
    }
    .portfolio-version-container{
        display: flex;
        flex-direction: column;
        color: black;

        h1{
            margin: 0;
            font-size: clamp(0.8rem, 6.5cqw, 1.5rem);
        }
        .version{
            font-size: clamp(0.6rem, 6.5cqw, 1rem);
            display: flex;
            justify-content: space-between;
        }
    }
}
.a_simple
{
    color: black;
    width: 100%;
    margin: 0;
    text-align: end;
    font-size: clamp(0.6rem, 3cqw, 1rem);
    font-weight: 500;
}
.stripe {
    width: 100%; /* adjust to your needs */
    height: 100%; /* height similar to the image */
    background: repeating-linear-gradient(
    -45deg,
    #000 0px,
    #000 12px,
    transparent 12px,
    transparent 20px
    );
}
.front-page{
    display: flex;
    flex-direction: row;
    color: black;
    padding-top: clamp(8px, 3cqw, 25px);
    padding-bottom: clamp(5px, 3cqw, 20px);
    border-bottom: 3px solid black;
    container-type: inline-size;
    gap: 10px;
    .qr-container{
        display: flex;
        width: 100%;
        container-type: inline-size;
    }
    .github_account_qr{
        aspect-ratio: 1/1;
    }
    h2{
        font-size: clamp(1rem, 8.5cqw, 3.7rem);
        margin: 0;
        height: fit-content;
        padding: 0;
        white-space: nowrap;
    }
    .basic-info{
        display: flex;
        flex-direction: column;
        padding: 0;
        margin-left: auto;
        width: 100%;
        line-height: 1;
        gap: clamp(5px, 6cqw, 25px);

        span{
            font-size: clamp(1rem, 10cqw, 2rem);
            font-weight: 500;
            white-space: nowrap;
            height: fit-content;

        }
        .bday-container{
            display: flex;
            flex-direction: row;
            gap: 10px;
            height: fit-content;
        }

        .box-x-container{
            display: flex;
            flex-direction: row;
            justify-content: space-between;
            width: 100%;
            height: 100%;
        }
        .x-box {
            width: clamp(20px, 10cqw, 100px);
            height: clamp(20px, 10cqw, 100px);
            border: 3px solid #000;
            position: relative;
            box-sizing: border-box;
        }


        .x-box::before,
        .x-box::after {
            content: "";
            position: absolute;
            top: 50%;
            left: 50%;
            width: 110%;
            height: 3px;
            background: #000;
            transform-origin: center;
        }


        .x-box::before {
            transform: translate(-50%, -50%) rotate(45deg);
        }


        .x-box::after {
            transform: translate(-50%, -50%) rotate(-45deg);
        }
    }
}
.random-hash{
    font-weight: 500;
    font-size: clamp(0.8rem, 3cqw, 1rem);
}
.summary-socials-container{
    display: flex;
    width: 100%;
    height: 100%;
    border: 3px solid black;
    flex-direction: column;
    .summary-head{
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        width: 100%;
        align-items: center;
        span{
            font-weight: 500;
            font-size: clamp(1rem, 3cqw, 2rem);
            line-height: 1;
            margin: 0;
            padding: 0;
            padding: 0px 20px;
        }
        .subtitle-container{
            display: flex;
            flex-direction: row;
            height: 100%;
            gap:2rem;
            justify-content: center;
            align-items: center;
            img{
                height: 65%;
                aspect-ratio: 1/1;
            }
            .subtitle{
                color: white;
                background-color: black;
                height: 100%;
                padding: 5px 15px;
                display: flex;
                justify-content: center;
                align-items: center;
                margin: 0;
                width: clamp(7rem, 3cqw, 10rem);
                font-size: clamp(0.9rem, 3cqw, 1.2rem);
            }
        }
    }
    p{
        font-weight: 500;
        text-align: justify;
        text-justify: inter-word;
        padding: 0px 20px;
        text-indent: 1rem;
        font-size: clamp(0.8rem, 3cqw, 1.1rem);
    }

}
.summary-bottom{
    margin-top: auto;
    border-top: 3px solid black;
    display: flex;
    flex-direction: row;
    height: clamp(2rem, 6cqw, 4rem);
    .bottom{
        border-right: 3px solid black;
    }
    div{
        aspect-ratio: 1/1;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
    }
}
.content-container{
    display: flex;
    flex-direction: column;
    height: 100%;
    width: 100%;
    border: 3px solid black;

    .content{
        display: flex;
        flex-direction: column;
        height: 100%;
        width: 100%;
        padding: 20px;
        overflow: auto;
        border-bottom: 3px solid black;
        gap: 10px;
        span {
            font-weight: 600;
            font-size: clamp(1rem, 3cqw, 2rem);
        }
        p{
            display: flex;
            flex-direction: row;
            font-size: clamp(0.8rem, 3cqw, 1.5rem);
            gap: 5px;
            margin: 0;
            margin-left: 10px;
            padding: 0;
            align-items: center;
            

            img{
                aspect-ratio: 1/1;
                height: clamp(0.5rem, 3cqw, 1rem)
            }
        }
    }

    .summary-head{
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        width: 100%;
        align-items: center;
        .selector{
            display: flex;
            flex-direction: row;
            height: 100%;
        }
        span{
            font-weight: 500;
            font-size: clamp(1rem, 3cqw, 2rem);
            line-height: 1;
            margin: 0;
            padding: 0;
            padding: 0px 20px;
        }
        .subtitle-container{
            display: flex;
            flex-direction: row;
            height: 100%;
            gap:clamp(0.8rem, 3cqw, 2rem);
            justify-content: center;
            align-items: center;
            img{
                height: 65%;
                aspect-ratio: 1/1;
            }
            .subtitle{
                color: white;
                background-color: black;
                height: 100%;
                padding: 5px 15px;
                display: flex;
                justify-content: center;
                align-items: center;
                margin: 0;
                width: clamp(6.5rem, 3cqw, 10rem);
                font-size: clamp(0.9rem, 3cqw, 1.2rem);
                font-weight: 500;
                outline: none;
                border: none;
                outline: 3px solid black;
            }
            .unselected {
                position: relative;
                color: black;
                background-color: #f2f2f2;
                overflow: hidden;
            }

            .unselected::after {
                content: "";
                position: absolute;
                left: 0;
                bottom: 0;
                width: 100%;
                height: 6px; /* stripe thickness */
                background: repeating-linear-gradient(
                    -45deg,
                    #000 0px,
                    #000 6px,
                    transparent 6px,
                    transparent 12px
                );
                clip-path: inset(0 100% 0 0);
                transition: clip-path 0.25s ease-out;
            }

            .unselected:hover::after {
                clip-path: inset(0 0 0 0);
            }
        }
    }
}
.socials{
    display: flex;
    flex-direction: row;
    margin-top: auto;
    height: 20%;
    width: 100%;
    max-height: 40rem;

    .socials-content{
        display: flex;
        flex-direction: column;
        width: 100%;
        height: 100%;

        .social-links{
            display: flex;
            flex-direction: row;
            padding: 10px;
            gap: clamp(1rem, 3cqw, 4rem);

            span{
                font-weight: 600;
                font-size: clamp(1rem, 3cqw, 2rem);
            }
  
            a, p{
                display: flex;
                flex-direction: row;
                font-size: clamp(0.8rem, 3cqw, 1.5rem);
                gap: 5px;
                margin: 0;
                padding: 0;
                color: black;
                text-decoration: none;
                align-items: center;
            }
            img{
                height: clamp(0.5rem, 3cqw, 1.6rem);
                aspect-ratio: 1/1;
            }
        }
        .social-data{
            display: flex;
            flex-direction: column;
        }
    }
    .random-square{
        display: flex;
        height: 100;
        aspect-ratio: 1/1;
        margin-left: auto;
        border-left: 3px solid black;
        padding: 10px;

        img {
            height: 100%;
            aspect-ratio: 1/1;
            max-height: 20rem;
        }
    }
  
}
@media(max-width: 1000px){
    body{
        height: 100%
    }
    #root {
        flex-direction: column;
    }
    #arrow-orient{
        transform: rotate(90deg);
    }
    .front-page{
        h2{
            white-space: normal;
        }
    }
    .social-links {
        flex-direction: column !important;
    }

}
